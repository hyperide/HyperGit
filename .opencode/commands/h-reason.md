---
description: "Reason explicitly with Haft before deciding or building"
---

# Reason

Use this command only when the operator explicitly invokes `/h-reason`.
Treat `$ARGUMENTS` as the reasoning request context.

Before doing any reasoning or calling Haft tools, read
`.agents/skills/h-reason/SKILL.md` completely from the repository checkout and
apply that canonical instruction body. Do not proceed from this summary alone.
If the file cannot be read, stop and report the missing local instruction file.

$ARGUMENTS
